# Robotic Surgery Interface

This repository contains a simple Java application demonstrating the
abstract factory pattern for a robotic surgery system. Users can
select between a German or Japanese robot configuration, and each
variant provides its own arm and vision subsystems with localized
behaviors.

All user-visible strings and identifiers have been translated to
English to make the project fully English‑language.